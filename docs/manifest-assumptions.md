# Manifest assumptions - gagoline.co.il

> GENERATED by `ops/sync-manifest.ps1`. Do not edit by hand - edit
> `Israeli services sites/roster/sites/gagoline.json` and re-run the sync.
>
> Last synced: 2026-09-06

`site.config.json` is imported by `lib/site-config.ts`, which means webpack bundles the
whole file into the client JavaScript. The roster's `_needsConfirmation` array is therefore
**deliberately stripped** during sync - internal project notes must not ship to visitors.

Stripping it silently would be the laundering trap recorded in
[business-facts.md](business-facts.md) section 0: a reader of the repo manifest alone sees a
clean, complete NAP and reasonably concludes it is confirmed. **It is not.** This file is
where those assumptions stay visible.

## Outstanding - 5 item(s)

**Nothing below may be presented as a confirmed business fact.** Anything depending on
these - a Business Profile, a directory citation, published hours - stays blocked until
the owner confirms. See [entity-profile.md](entity-profile.md).

- ⬜ foundedYear
- ⬜ brand.primary
- ⬜ brand.secondary
- ⬜ brand.accent
- ⬜ silos-migrate-in-phase-2 (services/cities live in lib/site-config.ts)
